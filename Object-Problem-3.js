const morning = {
    breakfast: 'waffles',
    weather: 'sunny',
    time: '7 am'
};


function objectLength(obj) {

   return Object.keys(obj).length


}

console.log(objectLength(morning))


// I first created an object called "morning" and gave it 3 key-value pairs. I then created a function with an object as a parameter. I then did some research and found 
// a length method for objects. 